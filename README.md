# OpenGL samples with Python

## Setup

1. Create virtual env: 
   ```shell
    sudo pip3 install virtualenv 
    virtualenv env
   ```
2. Install requirements: `pip3 install -r requirements.txt`
3. Install GLFW: `sudo apt install libglfw3`